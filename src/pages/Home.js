import React, { Component, Fragment } from 'react';
import { NavBar } from './components/';

class Home extends Component {
   render() {
      return (
         <Fragment>
            <NavBar/>
         </Fragment>      
      );
   };
};

export default Home;
